            ->add('{{ field["name"] }}', CheckboxType::class, array(
                'label' => '{{ field["label"] }}',
                {# check box type needs always to be not mandatory #}
                'required' => false
            ))