            ->add('{{ field["name"] }}', CheckboxType::class, array(
                'label' => '{{ field["label"] }}',
{% if field["required"] == false %}
                'required' => false,
{% endif %}
            ))