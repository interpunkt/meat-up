            ->add('{{ field["name"] }}', VichFileType::class, array(
                'label' => '{{ field["label"] }}',
{% if field["required"] == false %}
                'required' => false,
{% endif %}
                'allow_delete' => true,
                'download_link' => true,
{% if field["croppableHeight"] is defined %}
                'attr' => array(
                    'class' => 'croppable',
                    'data-name' => '{{ field["name"] }}',
                    'data-prefix' => '{{ entityClassName }}',
                    'data-height' => '{{ field["croppableHeight"] }}',
                    'data-width' => '{{ field["croppableWidth"] }}',
                    'data-x' => '{{ field["croppableX"] }}',
                    'data-y' => '{{ field["croppableY"] }}',
{% if field["croppableRatio"] is defined %}
                    'data-aspect-ratio' => {{ field["croppableRatio"] }},
{% endif %}
                )
{% endif %}
            ))