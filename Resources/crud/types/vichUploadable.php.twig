            ->add('{{ field["name"] }}', VichFileType::class, array(
                'label' => '{{ field["label"] }}',
{% if field["required"] == false %}
                'required' => false,
{% endif %}
                'allow_delete' => true,
                'download_link' => true,
            ))