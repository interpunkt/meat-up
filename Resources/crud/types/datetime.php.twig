            ->add('{{ field["name"] }}', DateTimeType::class, array(
                'label' => '{{ field["label"] }}',
{% if field["required"] == false %}
                'required' => false,
{% endif %}
            ))