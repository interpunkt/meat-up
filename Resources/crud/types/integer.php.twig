            ->add('{{ field["name"] }}', IntegerType::class, array(
                'label' => '{{ field["label"] }}',
{% if field["required"] == false %}
                'required' => false,
{% endif %}
            ))